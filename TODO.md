# TODO

## Deno KV による二次キャッシュ

現在はインメモリ `Map`（LRU, 256エントリ）のみ。デプロイ/再起動でキャッシュが消える。

Deno KV を二次キャッシュとして導入すれば、コールドスタート時の再生成を回避できる。

- 構成: インメモリ → Deno KV → 生成
- `expireIn` で自動 TTL 設定可能
- 画像サイズ 2-5KB なので 64KB/件の制限は問題なし
- HTTP レベルの CDN キャッシュ (`Cache-Control: immutable`) が効くため、優先度は低い
